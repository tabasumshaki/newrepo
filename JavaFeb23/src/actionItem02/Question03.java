package actionItem02;

public class Question03 {

	public static void main(String[] args) {
		
		
		// Trying to answer question number 3.
		
		// first of all we have to declare all the required
		// variables with their values.
		// so here except for age, all the other variables are string
		// such as --
		
		String name = "Shaki";
		String city = "Brooklyn";
		int age = 15;
		String college = "Prosmart";
		String profession = "QA tester";
		String animal = "cat";
		String petname = "Tom";
		
		// after declaring variables and values
		// its time to print the statement.
		// so for each of the line I have written a code here.
		// here the rule is: in the bracket we have to mention
		// string with "" and variables as it is named. 
		// and if we want to add string or variable
		// within the bracket, then we have to add them with + sign
		// such as (variable + "string" + "string" + variable + "string")
		
		
		System.out.println("Once there was a person named " 
		+ name + " who lived in " + city +".");
        System.out.println("At the age of " + age + ", " + name + 
        		" went to college at " + college + ".");
		System.out.println(name + " graduated and went to work as a "
				+ profession + ".");
        System.out.println("Then," + name + " adopted a(n) " + animal
        		+ " named " + petname + ".");
        
        
        
        
	}

}
